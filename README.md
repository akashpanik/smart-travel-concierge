# Smart Travel Concierge

A multi-agent CrewAI system that plans travel.
It extracts user preferences, recommends flights,
suggests hotels, and generates a full itinerary.

## Run
pip install -r requirements.txt
export OPENAI_API_KEY=xxxxx
python main.py
